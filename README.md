# .my_utils

## Dependencies

- zsh
- oh-my-zsh
- fzf
- git

## Setup

1. Create symbolic link to main zshrc:

```
ln -s ~/.my_util/zsh.d/zshrc ~/.zshrc
source ~/.zshrc
```

1. Export env for folder path

```
export SCRIPTS=$HOME/.my_utils/scripts
```
